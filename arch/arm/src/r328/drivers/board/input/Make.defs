ifeq ($(CONFIG_DRIVERS_R328_KEYBOARD),y)
include r328$(DELIM)drivers$(DELIM)board$(DELIM)input$(DELIM)keyboard$(DELIM)Make.defs
endif

ifeq ($(CONFIG_DRIVERS_R328_GPADC_KEY),y)
include r328$(DELIM)drivers$(DELIM)board$(DELIM)input$(DELIM)keyboard$(DELIM)Make.defs
endif
