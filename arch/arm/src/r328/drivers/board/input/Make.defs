ifeq ($(CONFIG_DRIVERS_INPUT),y)
CHIP_CSRCS += drivers/board/input/sunxi_input.c

ifeq ($(CONFIG_INPUT_TEST_DEMO),y)
CHIP_CSRCS += drivers/board/input/sunxi_input_test.c
endif

include r328$(DELIM)drivers$(DELIM)board$(DELIM)input$(DELIM)keyboard$(DELIM)Make.defs
endif


